See [AGENTS.md](AGENTS.md) — same instructions, one copy.

(coach-landing keeps this file as a symlink; Windows refused to create one here
without administrator privileges, so it is a pointer instead. If you ever run
with symlinks enabled, replacing this with a link to `AGENTS.md` is fine.)
