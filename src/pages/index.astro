---
import Page from '../components/Page.astro';

/**
 * Spanish, the default locale — served unprefixed at `/`.
 *
 * The page body lives in `src/components/Page.astro` and is shared with
 * `/en/`. Anything added here and not there is a difference between languages,
 * which is the thing to avoid.
 */
---

<Page />
