---
import Page from '../../components/Page.astro';

/**
 * English, served at `/en/`.
 *
 * The route's own directory is what sets `Astro.currentLocale`, which every
 * component reads for its copy — so this file needs nothing but the shared page.
 */
---

<Page />
